"""MCP servers exposed by the ETA workspace.

This package holds servers that expose internal ETA surfaces to MCP
clients (Hermes / Claude / other operators). The first inhabitant is
``jarvis_mcp_server`` — eleven JARVIS Supercharge tools wrapped behind
a token-gated stdio transport.
"""
